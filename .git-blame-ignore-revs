# Commits that changed formatting and nothing else.
#
# `git blame --ignore-revs-file .git-blame-ignore-revs` skips them, and so does
# GitHub's blame view without being asked. Configure it once locally with:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here that a reader would never want blame to stop on: a
# reformat, a rename, a licence header. Anything that changed behaviour belongs
# in the history where blame can find it.

# Apply rustfmt — 62 files, line breaks and indentation only (D77).
7dd19c1f87854e8579bd276c0be7fef3c3ea4131
